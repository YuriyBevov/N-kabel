"use strict";

/*(function () {
  const accept = document.querySelector('.active .modal__group--submit input');
  const btn = document.querySelector('.active .modal__group--submit button');
  console.log(btn)

  const onChangeHandler = function () {
    if (accept.checked) {
      console.log('ok')
    } else {
      console.log('wrong')
    }
  }

  const onClickHandler = function (evt) {
    console.log('&!')
    evt.preventDefault();
    if(accept.checked) {
      return
    } else {
      console.log('flase')
    }
  }

  if(!accept.checked) {
    console.log('not checked')
    accept.addEventListener('change', onChangeHandler);
  }

  btn.addEventListener('click', onClickHandler);
})();*/
